import React from "react";
import { Container } from "./ConfirmForm.styles";

type PropsType = {};

const ConfirmForm = ({}: PropsType) => {
  return <Container></Container>;
};

export default ConfirmForm;
